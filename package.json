{
  "name": "schema-form",
  "version": "0.1.0",
  "description": "turn a json schema into form fields",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tyler Bushnell <wunderlinks@gmail.com>",
  "license": "ISC",
  "browserify": {
    "transform": [
      "coffeeify",
      "cssify"
    ]
  },
  "devDependencies": {
    "browserify": "~2.35.1"
  },
  "dependencies": {
    "coffeeify": "~0.5.2",
    "cssify": "^0.6.0",
    "pikaday": "^1.2.0"
  }
}
